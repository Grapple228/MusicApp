namespace Shared;

public enum OperationId
{
    LoginClient,
    LoginServer,
    RegisterClient,
    RegisterServer,
    LogoutClient,
    SettingsClient,
    SettingsServer,
    TracksInfoClient,
    TracksInfoServer,
    CurrentTrackInfoClient,
    CurrentTrackInfoServer
}